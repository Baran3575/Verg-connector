
## BUILD RULES
- **NEVER** run `./gradlew build`, `./gradlew compileJava`, or any local build commands.
- ALL builds are done via GitHub Actions.
- Do NOT test compilation locally. Rely exclusively on GitHub Actions for validation.
